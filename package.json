{
  "name": "sf-unlucky-deploy",
  "version": "0.1.0",
  "description": "Salesforce 메타데이터를 비교하고 안전하게 검증·배포하는 CLI",
  "private": true,
  "type": "module",
  "bin": {
    "sfud": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json && node scripts/make-cli-executable.mjs",
    "check": "tsc --project tsconfig.json && tsc --project tsconfig.e2e.json && vitest run && tsc --project tsconfig.build.json && node scripts/make-cli-executable.mjs",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "typecheck": "tsc --project tsconfig.json && tsc --project tsconfig.e2e.json",
    "verify": "tsc --project tsconfig.json && tsc --project tsconfig.e2e.json && vitest run && playwright test && tsc --project tsconfig.build.json && node scripts/make-cli-executable.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "diff": "^9.0.0",
    "fast-xml-parser": "^5.11.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
